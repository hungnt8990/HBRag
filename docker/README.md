# Docker

The root `docker-compose.yml` starts local infrastructure for development:

- PostgreSQL
- Qdrant
- MinIO

Application Dockerfiles can be added here later if backend and frontend containers are needed for a target deployment.
