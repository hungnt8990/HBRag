diff --git a/app/api/routes/documents.py b/app/api/routes/documents.py
index 6cd3260..635743f 100644
--- a/app/api/routes/documents.py
+++ b/app/api/routes/documents.py
@@ -40,9 +40,11 @@ from app.services.chunking_service import (
     ChunkingService,
     DocumentChunkingError,
     DocumentChunkStatusError,
-    DocumentNotFoundError as ChunkDocumentNotFoundError,
     EmptyParsedTextError,
 )
+from app.services.chunking_service import (
+    DocumentNotFoundError as ChunkDocumentNotFoundError,
+)
 from app.services.document_parser_service import (
     DocumentFileNotFoundError,
     DocumentNotFoundError,
@@ -83,11 +85,13 @@ from app.services.permissions import (
 from app.services.storage import StorageClient, get_storage_client
 from app.services.vector_indexing_service import (
     DocumentChunksNotFoundError,
-    DocumentNotFoundError as VectorDocumentNotFoundError,
     DocumentVectorIndexStatusError,
     VectorIndexingError,
     VectorIndexingService,
 )
+from app.services.vector_indexing_service import (
+    DocumentNotFoundError as VectorDocumentNotFoundError,
+)
 from app.services.vector_store import QdrantVectorStore, get_vector_store
 
 router = APIRouter(prefix="/api/documents", tags=["documents"])
diff --git a/app/services/docling_v6_chunking.py b/app/services/docling_v6_chunking.py
index 336adf9..f6ee252 100644
--- a/app/services/docling_v6_chunking.py
+++ b/app/services/docling_v6_chunking.py
@@ -3,8 +3,9 @@ from __future__ import annotations
 import copy
 import re
 import sys
+from collections.abc import Iterable, Sequence
 from dataclasses import dataclass
-from typing import Any, Iterable, Literal, Sequence
+from typing import Any, Literal
 
 from docling.chunking import HybridChunker
 from docling_core.transforms.chunker.hierarchical_chunker import (
@@ -19,7 +20,6 @@ from docling_core.transforms.serializer.markdown import (
 from docling_core.types.doc import DoclingDocument
 from pydantic import Field
 
-
 DEFAULT_MAX_TOKENS = 350
 DEFAULT_CONTEXT_BUDGET = 80
 
@@ -385,7 +385,7 @@ def normalize_markdown_table(text: str) -> str:
     normalized: list[str] = []
     table_separator_seen = False
     previous_was_table = False
-    for idx, line in enumerate(intermediate):
+    for _idx, line in enumerate(intermediate):
         if not line.strip():
             normalized.append("")
             previous_was_table = False
diff --git a/app/services/rag_chunk.py b/app/services/rag_chunk.py
index 99ee4c5..badda75 100644
--- a/app/services/rag_chunk.py
+++ b/app/services/rag_chunk.py
@@ -351,7 +351,7 @@ def rag_chunk_from_database(
         record["issuer"] = document_metadata.get("issuer")
     rag_chunk = rag_chunk_from_record(
         record,
-        document_id=getattr(document, "id"),
+        document_id=document.id,
         source_file=source_file,
         source_uri=source_uri,
         document_title=getattr(document, "title", None),
@@ -368,19 +368,19 @@ def rag_chunk_from_database(
     )
     return rag_chunk.model_copy(
         update={
-            "database_chunk_id": str(getattr(chunk, "id")),
+            "database_chunk_id": str(chunk.id),
             "organization_id": (
-                str(getattr(document, "organization_id"))
+                str(document.organization_id)
                 if getattr(document, "organization_id", None)
                 else None
             ),
             "knowledge_base_id": (
-                str(getattr(document, "knowledge_base_id"))
+                str(document.knowledge_base_id)
                 if getattr(document, "knowledge_base_id", None)
                 else None
             ),
             "uploaded_by_user_id": (
-                str(getattr(document, "uploaded_by_user_id"))
+                str(document.uploaded_by_user_id)
                 if getattr(document, "uploaded_by_user_id", None)
                 else None
             ),
diff --git a/app/services/vector_indexing_service.py b/app/services/vector_indexing_service.py
index ed22c75..5efabeb 100644
--- a/app/services/vector_indexing_service.py
+++ b/app/services/vector_indexing_service.py
@@ -292,7 +292,7 @@ class VectorIndexingService:
         kwargs: dict[str, Any] = {}
         if "tenant_id" in parameters:
             kwargs["tenant_id"] = getattr(document, "organization_id", None)
-        await delete_method(getattr(document, "id"), **kwargs)
+        await delete_method(document.id, **kwargs)
 
     async def _get_primary_document_file(self, document_id: UUID) -> Any | None:
         getter = getattr(self._repository, "get_primary_document_file", None)
diff --git a/tests/test_vector_indexing.py b/tests/test_vector_indexing.py
index 1dad1b0..b1643a5 100644
--- a/tests/test_vector_indexing.py
+++ b/tests/test_vector_indexing.py
@@ -10,6 +10,8 @@ from app.api.routes.documents import (
 )
 from app.api.routes.search import (
     get_embedding_provider as get_search_embedding_provider,
+)
+from app.api.routes.search import (
     get_vector_store as get_search_vector_store,
 )
 from app.main import app
